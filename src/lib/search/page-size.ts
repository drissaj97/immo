/** Taille de page résultats — première page rapide, suite en progressive load. */
export const LISTINGS_PAGE_SIZE = 10;

/** Pins carte — volume plus large que la grille. */
export const MAP_PAGE_SIZE = 80;
